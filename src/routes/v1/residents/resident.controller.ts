import { Request, Response } from "express";

export const createResident = (req: Request, res: Response) => {
  res.json({});
};

export const listResident = (req: Request, res: Response) => {
  res.json({});
};

export const getResident = (req: Request, res: Response) => {
  res.json({});
};

export const updateResident = (req: Request, res: Response) => {
  res.json({});
};

export const deleteResident = (req: Request, res: Response) => {
  res.json({});
};
